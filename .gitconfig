[user]
    name = arurke
[alias]
    ap = add -p
    a = add
    cm = "!git commit -m \"$*\" && :"
    co = checkout
    cob = checkout -b
    d = diff
    ds = diff --staged
    pushf = push --force-with-lease
    s = status
[core]
    editor = vim
[rebase]
    autosquash = true
